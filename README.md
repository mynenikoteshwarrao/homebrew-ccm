# homebrew-ccm

Homebrew tap for [ccm](https://github.com/mynenikoteshwarrao/claude-context-manager) — Claude Context Manager.

## Install

```bash
brew install mynenikoteshwarrao/ccm/ccm
```

The formula in `Formula/ccm.rb` is auto-updated by the `release` workflow in the main repo on each tag push.
